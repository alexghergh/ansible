- name: Ensure git config file does not exist already.
  file:
    path: "{{ xdg_config }}/git/config"
    state: absent

- name: Stow the git configs.
  become_user: "{{ user }}"
  command:
    cmd: 'stow --restow --verbose --dir="{{ home }}/.dotfiles" --target="{{ home }}" git'
    chdir: "{{ home }}/.dotfiles"
